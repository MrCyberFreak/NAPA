"""Schedule parser (fixtures: who plays whom each round).

Stub — implemented in Phase 1 after the roster parser is verified. Pin to a
captured `print_schedule_v1.php` fixture before wiring to live data.
"""

from __future__ import annotations


def parse_schedule(html: str):
    raise NotImplementedError("schedule parser pending a captured fixture")
