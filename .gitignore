# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Data: the SQLite DB is a build artifact regenerable from the archive.
# The raw HTML archive (data/raw/) IS committed — the cloud scrape pushes it
# back as the durable historical record.
data/napa.db

# OS
.DS_Store
data/*.db
data/*.db-journal

# Incremental profile-ingest change-detection state (regenerable; tools/ writes it)
data/*ingest_state.json

# generated dashboard output (regenerate via tools/dashboard.py)
dashboard.html

# Local session scratch (handoff docs + logs) and DB backups — not shipped.
_handoffs/
data/napa.db.bak*

# Local editor/tooling output — not part of the project.
.codescope/

# Local daily-scrape mirror logs (tools/local_scrape.ps1) — never shipped.
logs/

# Design drop-in archive (the extracted UI lives under handoff_napa_web/) — not shipped.
NAPA Mobile UI.zip
handoff_napa_web/

# Cloned sibling GitHub repos for reference (PoolPredict UI) — never committed.
_ref/

# PoolPredict Scout UI brought in as the NAPA frontend prototype (real rosters/history) — not shipped yet.
web/

# scratch - not source
.tmp/
