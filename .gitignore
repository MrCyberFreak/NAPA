# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Data: the SQLite DB is a build artifact regenerable from the archive.
# The raw HTML archive (data/raw/) IS committed — the cloud scrape pushes it
# back as the durable historical record.
data/napa.db

# OS
.DS_Store
