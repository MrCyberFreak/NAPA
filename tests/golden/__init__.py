# Golden-dataset regression harness for the parse->DB pipeline.
# See harness.py for the shared scoped builder + extractor shared by the
# capture tool (tools/golden_capture.py) and the test suite
# (tests/test_golden_harness.py).
